/**
 * Displays registered liquidity pools and their APYs.
 *
 * TODO: wire usePools() once the contract hook is implemented.
 */
export function PoolList() {
  return (
    <div className="card">
      <h2>Liquidity Pools</h2>
      <p className="muted">
        Implement usePools() in hooks/useContracts.ts to display registered
        pools and their APYs.
      </p>
      {/* TODO: render pool table once usePools is implemented */}
    </div>
  );
}
